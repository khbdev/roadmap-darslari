# Go + PostgreSQL CRUD API

A simple CRUD API built with Go and PostgreSQL. This project is based on the RESTful API concept and manages the **users** resource.

## 🔹 Technologies

* **Go (Golang)** – Backend programming language
* **PostgreSQL** – Database
* **net/http** – HTTP server
* **database/sql** – Working with SQL
* **JSON** – Data exchange format

